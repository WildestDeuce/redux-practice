import React from 'react';
// import { useSelector } from 'react-redux';

// const counter = useSelector(state => state.counter);
// const isLoggedIn = useSelector(state => state.isLogged);

function App() {
  return (
    <div className="App">
      <h1>Counter  </h1>
      <h3>This info should be hidden</h3>
    </div>
  );
}

export default App;
